export enum tagTypes {
  buyer = "buyer",
  seller = "seller",
  user = "user",
  profile = "profile",
  category = "category",
  auth = "auth",
}

export const tagTypesList = [
  tagTypes.buyer,
  tagTypes.seller,
  tagTypes.user,
  tagTypes.category,
  tagTypes.profile,
  tagTypes.auth,
];
